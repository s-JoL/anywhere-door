# 任意门 / Anywhere Door — 环境变量示例
# 复制为 .env.local 后填写。该文件被 .gitignore 忽略，绝不会提交。
#
# OPENROUTER_API_KEY 是 **仅限开发环境** 的便利回退：
# 在本地 `npm run dev` 时，若请求未带 key，且 provider 为 openrouter，
# 则使用此 key，省去每次在 /settings 里粘贴。
#
# 生产部署不会读取此回退 —— 部署版严格 BYO-key（自带 key），
# 访客必须在应用内 /settings 页面填写自己的模型 key。
# 详见 src/lib/llm/resolve-key.ts。
OPENROUTER_API_KEY=
